#include <stdio.h>
#include <string.h>
#include <omnetpp.h>

using namespace std;
class Recordv{
public:
    int a;
    void saveData();

    protected:
        int test();
//        virtual void initialize();
//        virtual void handleMessage(cMessage *msg);
};

//Define_Moudle(recordf);

void ::saveData(){
    cout<< "saveData!!" << endl;
}
